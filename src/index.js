import React from 'react';
import ReactDOM from 'react-dom/client';
import { ParqueApp } from './ParqueApp';


ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ParqueApp />
    </React.StrictMode>
)